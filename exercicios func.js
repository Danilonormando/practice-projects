const balance = 100;

function sumBalance(value) {
  return value;
}

function subBalance(value) {
 return value;
}

function multBalance(value) {
 return value;
}

function divBalance(value) {
 return value;
}
let FinalBalance = balance + sumBalance(1000) - subBalance(50) * multBalance(2) / divBalance(2);

console.log(FinalBalance);